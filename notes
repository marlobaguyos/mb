echo "# pangitpogi" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin url ------Only when you need to add the remote location
git push -u origin main